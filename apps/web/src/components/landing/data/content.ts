/**
 * @module landing/data/content
 * @description Non-CMS constants. Marketing copy lives in Sanity.
 */

/** Default visitor marquee count when PostHog server keys are unset. */
export const VISITOR_COUNT = 12_438;
